import { Fragment } from 'react';
import styles from './MonthStatistics.module.scss';

export default function MonthStatistics() {

   return (
      <Fragment>
         <div className={styles.home}>MonthStatistics</div>
      </Fragment>
   );
}